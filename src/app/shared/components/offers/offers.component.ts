import { Component } from '@angular/core';

@Component({
  selector: 'app-offers',
  templateUrl: './offers.component.html',
  styleUrl: './offers.component.scss'
})
export class OffersComponent {

  // Implementar llamada a API para traer publicaciones de Instagram

//   images = [944, 1011, 984].map((n) => `https://picsum.photos/id/${n}/1920/1080`);
//   currentSlide: number = 1;

  // Para implementar botones
  // next(){
	// 	let currentSlide = (this.currentSlide + 1) % this.images.length;
	// 	this.jumpToSlide(currentSlide);
	// }

	// previous(){
	// 	let currentSlide = (this.currentSlide - 1 + this.images.length) % this.images.length;
	// 	this.jumpToSlide(currentSlide);
	// }

	// jumpToSlide(index: number){
	// 	setTimeout(() => {
	// 		this.currentSlide = index;
	// 	}, 250);
	// }
}
